# Migration `20201212172904-add-transactions-to-db`

This migration has been generated by Adam Siekierski at 12/12/2020, 6:29:04 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Transaction" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "name" TEXT NOT NULL,
    "description" TEXT NOT NULL,
    "userId" INTEGER NOT NULL,

    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201208200003-add-email-hash..20201212172904-add-transactions-to-db
--- datamodel.dml
+++ datamodel.dml
@@ -1,23 +1,24 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url = "***"
 }
 generator client {
   provider = "prisma-client-js"
 }
 model User {
-  id             Int       @id @default(autoincrement())
-  createdAt      DateTime  @default(now())
-  updatedAt      DateTime  @updatedAt
+  id             Int           @id @default(autoincrement())
+  createdAt      DateTime      @default(now())
+  updatedAt      DateTime      @updatedAt
   name           String?
-  email          String    @unique
+  email          String        @unique
   emailHash      String?
   hashedPassword String?
-  role           String    @default("user")
+  role           String        @default("user")
   sessions       Session[]
+  transactions   Transaction[]
 }
 model Session {
   id                 Int       @id @default(autoincrement())
@@ -31,4 +32,13 @@
   antiCSRFToken      String?
   publicData         String?
   privateData        String?
 }
+
+model Transaction {
+  id          Int      @id @default(autoincrement())
+  createdAt   DateTime @default(now())
+  user        User     @relation(fields: [userId], references: [id])
+  name        String
+  description String
+  userId      Int
+}
```


